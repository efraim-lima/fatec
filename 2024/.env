# Exemplo do arquivo .env
REPO="https://www.github.com/efraim-lima/fatec"
PYTHON_EXECUTABLE="/bin/env python"
OS_NAME=$(uname -s | tr '[:upper:]' '[:lower:]')
USER=$USER
INIT_DIR=$HOME
ACTUALL=$PWD
CAMINHO=$(sudo find / -type d -name "malwares")
START=$(sudo find / -type f -name "start_play.py")
VENV_DIR=$CAMINHO

#Para chamar a .env nos arquivos:

#Python:

# # main.py (seu código principal da aplicação)

# import os
# from dotenv import load_dotenv

# # Carrega as variáveis de ambiente do arquivo .env
# load_dotenv()

# # Exemplo de uso das variáveis de ambiente
# nome_usuario = os.getenv("NOME_USUARIO")

#Shell

# source .env

# # Como importar as variaveis no arquivo:
# $VARIAVEL
